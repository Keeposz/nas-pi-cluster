---
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    resources:
      requests:
        memory: "96Mi"
        cpu: "50m"
      limits:
        memory: "192Mi"
